<?php
define('PUSHER_APP_ID', trim(getenv('PUSHER_APP_ID')));
define('PUSHER_KEY', trim(getenv('PUSHER_KEY')));
define('PUSHER_SECRET', trim(getenv('PUSHER_SECRET')));
define('PUSHER_CLUSTER', trim(getenv('PUSHER_CLUSTER')));
?>
